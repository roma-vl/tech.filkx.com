server {
    listen 80;
    charset utf-8;
    index index.html;
    root /app/dist;
    server_tokens off;
    client_max_body_size 2G;

    location / {
        try_files $uri $uri/ /index.html;
    }

    add_header X-Frame-Options "SAMEORIGIN";
}
